{
  "name": "ReNoMy",
  "version": "0.1.5",
  "private": true,
  "scripts": {
    "dev": "concurrently \"yarn start-app\" \"yarn start-api\"",
    "start-app": "yarn workspace app start",
    "start-api": "yarn workspace api start"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
